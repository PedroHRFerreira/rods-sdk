# Rods Hook Base

Use Context Engine before broad file reads. Search first, read only matching chunks, and ingest relevant fallback files when the index was stale.

Use RTK when available for noisy shell output such as tests, logs, diffs, and broad searches.

If a card or link implies external dependencies, ask whether to proceed, only plan, or take another action before running Context Engine search.
