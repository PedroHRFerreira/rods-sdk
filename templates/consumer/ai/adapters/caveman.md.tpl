# Caveman Adapter

Purpose: reduce agent response verbosity when explicitly requested.

Use as opt-in only:

- `brief`: concise final answers.
- `review`: terse review comments.
- `commit`: short commit messages.

Never compress code, commands, logs, errors, diffs, paths, URLs, or generated artifacts.

