# Claude-Mem Adapter

Purpose: preserve compressed memory across sessions and days.

Recommended setup:

```bash
npx claude-mem install
```

Use this adapter only when the task depends on historical decisions or prior-session observations. It does not replace Context Engine project indexing.

